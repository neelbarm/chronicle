# docs

`screenshot.png` is referenced from the project README. Drop a capture of the
generated report here (the hero or the hotspots treemap both work well) to fill
it in — `npm run demo` writes reports you can screenshot straight from
`examples/`.
